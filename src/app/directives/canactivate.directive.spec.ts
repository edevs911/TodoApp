import { CanactivateDirective } from './canactivate.directive';

describe('CanactivateDirective', () => {
  it('should create an instance', () => {
    const directive = new CanactivateDirective();
    expect(directive).toBeTruthy();
  });
});
